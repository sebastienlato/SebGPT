# Architectural Decision Log

This is an append-only record. Add new entries to supersede earlier decisions; do not delete or silently rewrite historical entries. Corrections should be added as clearly labeled amendments.

## DEC-0001 — Learning-first implementation strategy

- **Date:** 2026-09-06
- **Status:** Accepted
- **Decision:** Build a small GPT-style language model progressively from first principles in Python and PyTorch. PyTorch may supply tensors, autograd, optimization, and low-level neural-network primitives, but the educational architecture will be implemented explicitly before higher-level frameworks are considered.
- **Why:** The project's purpose is to understand modern language models, not to maximize model capability or implementation speed.
- **Alternatives considered:** Wrap a pretrained model or API; use a high-level training/model framework; begin with a complete Transformer and explain it afterward.
- **Consequences:** Progress will be deliberately phased. Some standard components will be reimplemented for clarity. Core architecture changes require explicit discussion and a new decision entry.

## DEC-0002 — Repository as continuity authority

- **Date:** 2026-09-06
- **Status:** Accepted
- **Decision:** Treat repository documentation and code as the authoritative source of truth across sessions, with `PROJECT_STATE.md` as the primary current-state anchor.
- **Why:** The project is expected to span many weeks or months and multiple Work and Codex sessions; chat history alone is not a durable or deterministic handoff mechanism.
- **Alternatives considered:** Rely on conversation history; maintain a single unstructured project note; rely on code alone.
- **Consequences:** Sessions must follow `docs/SESSION_PROTOCOL.md`. Current state, durable decisions, learning notes, and experiments have distinct documents and update rules.

## DEC-0003 — Phase gates prevent premature implementation

- **Date:** 2026-09-06
- **Status:** Accepted
- **Decision:** Use the ordered phases and exit criteria in `ROADMAP.md` as learning gates. Do not start later-phase implementation until the current phase is verified and its completion is recorded.
- **Why:** A working advanced implementation could conceal gaps in the foundational concepts the project exists to teach.
- **Alternatives considered:** Build a complete mini-GPT immediately and study components afterward; allow agents to choose implementation order opportunistically.
- **Consequences:** Work may pause for review between phases. The exact next step must remain explicit in `PROJECT_STATE.md`, and exceptions require a recorded decision.

## DEC-0004 — Native Python.org runtime and project-local virtual environment

- **Date:** 2026-09-06
- **Status:** Accepted
- **Decision:** Use the existing native ARM64 Python.org CPython 3.14.4 installation to create a `.venv` virtual environment in the project root. Do not add a Python version manager while the installed interpreter remains suitable.
- **Why:** The interpreter is already installed, native to the Apple Silicon architecture, includes `venv` and pip, and is supported by the selected stable PyTorch wheel. A project-local environment provides dependency isolation without introducing another management layer.
- **Alternatives considered:** Use Apple's Xcode-managed Python 3.9.6; install Homebrew Python; install pyenv, uv, Conda, or Mamba.
- **Consequences:** Development commands run through `.venv`, which is excluded from Git. Reproducing this verified environment requires CPython 3.14.4; a future interpreter change requires a new decision and full verification.

## DEC-0005 — Minimal pinned dependency workflow

- **Date:** 2026-09-06
- **Status:** Accepted
- **Decision:** Declare only `torch==2.14.0` in `requirements.in` and record the complete resolved environment in `requirements.lock`. Use Python's standard-library `unittest` framework initially. Do not add NumPy or other optional packages until an approved learning phase requires them.
- **Why:** Separating the direct requirement from the resolved lock keeps dependency intent visible while allowing exact environment reproduction. PyTorch 2.14.0 provides a stable CPython 3.14, macOS 14+, ARM64 wheel and supplies the low-level tensor, autograd, optimization, and device features SebGPT needs.
- **Alternatives considered:** Keep only an unpinned requirements file; use only a full `pip freeze`; introduce a packaging or lock-file tool; add common ML, notebook, plotting, or testing packages preemptively.
- **Consequences:** PyTorch's transitive packages are pinned even though they are not direct SebGPT choices. PyTorch may warn that optional NumPy interoperability is unavailable; this does not affect the verified tensor, autograd, CPU, or MPS operations. Dependency changes require explicit approval and lock regeneration.

## DEC-0006 — Git baseline and generated-artifact policy

- **Date:** 2026-09-06
- **Status:** Accepted
- **Decision:** Initialize the repository on a `main` branch after the Phase 0 environment is verified, then create one baseline commit containing the project scaffold, documentation, dependency manifests, and environment test. Exclude the virtual environment, caches, local datasets, checkpoints, model-weight files, and generated experiment artifacts.
- **Why:** The baseline gives later educational work a clean, reviewable starting point without storing machine-local or large generated content.
- **Alternatives considered:** Commit before environment verification; defer Git initialization; commit the virtual environment or generated artifacts.
- **Consequences:** Empty source directories use tracked `.gitkeep` placeholders. Dataset provenance and experiment metadata may later be committed, but downloaded data, checkpoints, weights, and generated artifacts remain untracked.

## DEC-0007 — First corpus source, manifest, and leakage boundary

- **Date:** 2026-09-06
- **Status:** Accepted
- **Decision:** Build the first corpus from the UTF-8 plain-text artifact for Project Gutenberg eBook #100, *The Complete Works of William Shakespeare*. Target approximately 170,000 words using a fixed eight-play manifest. Train on *Hamlet*, *Romeo and Juliet*, *Macbeth*, *A Midsummer Night's Dream*, *Much Ado About Nothing*, and *Henry V*; validate on *The Tempest*; and reserve *Twelfth Night* for test. Split only at whole-work boundaries. Derive the initial vocabulary from training data only, audit validation/test for unseen characters before tokenization, and report those characters without modifying or adding them.
- **Why:** A single Gutenberg source provides consistent editorial conventions and clearer provenance than the canonical third-party Tiny Shakespeare copy. Multiple plays provide about one million characters of controlled genre variety, while whole-work evaluation tests transfer to unseen text and avoids overlapping-passage leakage. Training-only vocabulary construction prevents validation/test information from influencing learned preprocessing.
- **Alternatives considered:** Use the canonical Tiny Shakespeare file; use one Project Gutenberg novel; use a TinyStories subset; download separate Shakespeare editions; split a concatenated corpus by random or contiguous character ranges; derive the character vocabulary from all splits.
- **Consequences:** Validation and test measurements will be noisier and harder than within-work evaluation. Source acquisition, extraction, counts, hashing, and unseen-character handling require explicit later gates. Validation/test-only characters remain unchanged and may cause an unresolved representation problem until Phase 2 makes a visible decision.

## DEC-0008 — Raw-source preservation and Phase 1 boundary clarifications

- **Date:** 2026-09-06
- **Status:** Accepted; amends DEC-0006 and clarifies DEC-0007
- **Decision:** Git-track exactly `data/raw/gutenberg-ebook-100/complete-works.txt`, the unmodified small public-domain plain-text master acquired for the approved Phase 1 corpus. This is the sole exception to DEC-0006's general dataset-ignore policy and does not authorize any other raw source, processed dataset, generated metadata, or future dataset. Preserve its bytes from Git line-ending conversion. Use `docs/data/shakespeare-eight-play-manifest.json` as the authoritative machine-readable record for concrete provenance and processing facts. Phase 1 defines a training-only Unicode character inventory, not a token vocabulary or tokenizer. Automated pre-evaluation access to *Twelfth Night* is limited to approved structural markers and machine-readable code-point diagnostics without surrounding prose; Phase 2 must choose a generic unseen-input policy that is not tailored to observed test characters.
- **Why:** A mutable upstream URL plus a hash can verify bytes but cannot recover them. The narrow Git exception makes the exact educational source reproducible for another person without weakening the default large-data policy. Precise character terminology and test-access limits preserve the Phase 2 learning gate and reduce evaluation leakage.
- **Alternatives considered:** Keep the raw file ignored and rely on the mutable upstream URL; use an external content-addressed archive; broadly allow raw datasets in Git; treat the Phase 1 character inventory as a tokenizer vocabulary; expose test context during the unseen-character audit.
- **Consequences:** The raw master and completed provenance manifest must be committed together after authorized acquisition and verification. All processed data and unlisted dataset files remain ignored. Generated local metadata is subordinate to the tracked manifest. Extraction rules still require a post-acquisition design review, and no tokenization or unseen-input representation decision is made in Phase 1.

## DEC-0009 — Deterministic eight-play extraction policy

- **Date:** 2026-09-06
- **Status:** Accepted
- **Decision:** Extract each approved play from the pinned raw master by exact full-line body-title and successor-title markers, using recorded absolute positions only as assertions. Include the top-level title; remove the play-local range from the first exact `Contents` line through immediately before the first exact `Dramatis Personæ` line; retain `Dramatis Personæ` and all subsequent dramatic structure and text. Exclude only the consecutive empty CRLF separator lines before the successor marker, then convert retained CRLF pairs to LF with no other normalization. Emit eight separate split-assigned documents and deterministic provenance metadata. Apply the same algorithm to every split and restrict *Twelfth Night* diagnostics under the sealed-test contract. This decision approves the extraction contract as policy but does not authorize implementation.
- **Why:** Exact structural anchors are more robust and auditable than hard-coded line slicing, while assertions against the pinned source detect drift. Removing duplicated local contents avoids training on an artificial index; retaining character and dramatic structure preserves meaningful context. Separate documents enforce whole-work split boundaries without making tokenization decisions.
- **Alternatives considered:** Select by absolute line numbers; omit the work title; retain local contents; remove `Dramatis Personæ`; collapse or strip whitespace; concatenate training plays; special-case validation or test extraction.
- **Consequences:** Extraction must fail on hash, marker-cardinality, ordering, whitespace-boundary, overlap, or verification mismatches. Processed files remain ignored and reproducible from the tracked raw source, approved contract, implementation revision, and recorded hashes. Character inventories remain a later Phase 1 diagnostic; token units, token vocabulary, unseen-input handling, context windows, and all tokenizer behavior remain Phase 2 decisions.

## DEC-0010 — Exact structural preflight anchors and assertions

- **Date:** 2026-09-06
- **Status:** Proposed clarification; under review
- **Decision:** Define raw full-line matching as exact, case-sensitive Unicode code-point equality after strict UTF-8 decoding and removal of only the terminating CRLF. Pin the exact Gutenberg START/END values, the global heading as 20 U+0020 spaces plus `Contents`, and the complete ordered 44-entry region with a four-space prefix per entry. Distinguish play-local `Contents` as the zero-indent exact line. Retain the generic backward empty-line scan for end selection while requiring the pinned per-work successor-separator counts as preflight assertions: four CRLF-only empty lines for every selected work except *Macbeth*, which requires five; every selected work requires zero whitespace-only separator lines. Other title-surrounding blank-line observations remain informational only. Absolute positions remain assertions and never selection inputs.
- **Why:** A fresh implementation must not invent wrapper strings, trim markers, infer the global contents semantically, or decide whether observed separator counts matter. Exact values make preflight mechanical without changing the approved extraction outputs.
- **Alternatives considered:** Trim lines before marker matching; identify the global contents by the word `Contents` alone; infer its entries from uppercase or Shakespeare semantics; treat all blank-line observations as informational; select boundaries by absolute line or byte offsets.
- **Consequences:** Any mismatch in exact markers, ordered contents membership, required occurrence counts, or successor-separator assertions stops preflight. Whitespace-only lines remain distinct from empty CRLF-only lines. The clarification adds verification data only and does not authorize extraction, change the eight-work manifest, expose test prose, create a character inventory, or make a tokenization decision.
- **Status update (2026-09-06):** Accepted without changes. The original proposed status above is retained as append-only history; this update is the current status.

## DEC-0011 — Source-faithful Dramatis marker and provenance hash stages

- **Date:** 2026-09-06
- **Status:** Proposed clarification; under review
- **Decision:** Store `Dramatis Personæ` once as the shared canonical marker text and store only `dramatis_leading_u0020_count` per selected work. Construct each exact marker as that many U+0020 spaces followed by the canonical text, with no trailing spaces or matching transformations, and require exactly one occurrence ordered after the local zero-indent `Contents` marker and before the outer end. Define three distinct provenance hashes: the contiguous outer raw range before local-contents removal, including that removable range; the concatenated retained raw range after removal and before newline conversion; and the final processed UTF-8 bytes after CRLF-to-LF conversion only.
- **Why:** The tracked source uses zero indentation for six selected works and one leading U+0020 for *Romeo and Juliet* and *Twelfth Night*. A shared marker plus per-work indentation preserves exact source bytes without duplicated strings, stripping, normalization, or guessed exceptions. Named hash stages prevent different byte domains from being compared or recorded as if they were interchangeable.
- **Alternatives considered:** Require an inaccurate zero-indent marker for all works; strip marker whitespace; duplicate the full marker string per work; record only outer and processed hashes without defining the intermediate retained bytes.
- **Consequences:** In-memory extraction must build the marker mechanically from authoritative configuration and fail on any other indentation, trailing space, cardinality, or order. Per-work provenance must separately record outer-raw, retained-raw, and processed hashes and byte counts. This clarification does not change the raw source, work manifest, splits, title/successor markers, global contents, separator assertions, preflight behavior, or tokenization boundaries, and it does not authorize implementation.
- **Status update (2026-09-06):** Accepted without changes. The original proposed status above is retained as append-only history; this update is the current status.

## DEC-0012 — Transactional processed-dataset publication

- **Date:** 2026-09-07
- **Status:** Accepted
- **Decision:** Publish only an already accepted `tuple[ExtractedWork, ...]` and use the authoritative tracked manifest solely to verify dataset identity, exact work/order/split/path assignments, the accepted zero-candidate audit, and populated expected-result ledger values. Encode each `processed_text` directly as strict UTF-8 without transformation. For a first publication, validate every input and expected byte in memory. Open the repository `data` directory without following symlinks and verify its descriptor identity; create each staging directory exclusively relative to a retained, verified parent descriptor, then create every staged file with descriptor-relative `O_CREAT | O_EXCL | O_NOFOLLOW`, verify its regular-file type, and write/read back the exact bytes through that new descriptor. Revalidate every retained directory binding before path-based defense-in-depth verification. Build the complete future `data/processed` tree under the fixed `data/.processed-staging` path, then atomically promote that staging root to `data/processed` with macOS `renameatx_np` using `RENAME_EXCL` and `RENAME_NOFOLLOW_ANY`, relative to an open, identity-verified `data` directory descriptor. The exclusive kernel operation fails if a target wins the race and never replaces it. Capture the `data` and staging device/inode identities when staging is created and use descriptor-relative, no-follow checks to recognize substitution. Darwin provides no inode-conditional or descriptor-only unlink/rmdir operation; therefore a handled transaction failure performs no automatic staging deletion. It preserves the entire failed or suspicious staging state for explicit human resolution rather than risking deletion of an entry or root replaced after an identity check. An exact existing tree is an idempotent no-op; any mismatch, missing or unexpected entry, partial tree, symlink, or pre-existing staging tree fails without overwrite, repair, merge, or deletion of unexplained state. The generated `processing-manifest.json` is deterministic, contains no timestamp or machine-local paths, and remains an ignored derivative subordinate to the tracked manifest. This decision authorizes implementation and temporary-directory tests only; production publication remains separately gated.
- **Why:** Publication should demonstrate how accepted in-memory documents become durable artifacts without duplicating extraction logic, changing bytes, weakening sealed-test controls, or leaving a partially valid dataset after an ordinary failure. Refusing unexplained existing state keeps the small educational pipeline inspectable and makes idempotence explicit.
- **Alternatives considered:** Write files directly into the final tree; overwrite or repair mismatches; atomically replace individual files; regenerate extraction inside the publisher; treat processed files or the generated manifest as authoritative; include timestamps or discovered host details in generated output.
- **Consequences:** The publisher depends only on accepted extracted works plus manifest-declared publication facts and complete expected ledger values. Production publication cannot run until those ledger prerequisites and a committed publisher revision are recorded. Processed outputs remain ignored and reproducible. Any failed publication can leave staging residue that requires explicit human resolution; this is intentional because cleanup completeness is subordinate to identity-safe deletion. Any later automatic cleanup, force/repair mode, or alternate artifact format requires a new decision.

### Accepted amendment (2026-09-07) — Cooperative-workspace threat model

- **Discovery:** Supported Darwin APIs do not provide an atomic operation that both creates a directory and returns a descriptor bound to that exact new directory. `mkdirat` followed by `openat` has a namespace-substitution window. Darwin `renameatx_np` with `RENAME_EXCL` atomically protects the destination but identifies the source by name and cannot condition promotion on a previously verified source inode. The specified directory-tree architecture therefore cannot guarantee safety against an unrelated same-user process deliberately mutating repository namespace entries at those exact windows.
- **Amended threat model:** The accepted guarantee covers Model A, an ordinary/static SebGPT workspace, and Model B, multiple cooperating SebGPT publisher invocations that all obey the repository-local exclusive publication lock. Model C, adversarial namespace mutation by an unrelated process with the same filesystem authority that ignores the protocol, is explicitly outside the publisher guarantee.
- **Lock protocol:** Acquire the empty `data/.publish-lock` presence file atomically and relative to the verified `data` directory with `O_CREAT | O_EXCL | O_NOFOLLOW | O_CLOEXEC`, then hold its descriptor for the complete publication transaction. A cooperating publisher refuses an existing lock without creating staging or target state. Release the verified lock after ordinary success or a clean pre-publication refusal. Preserve the lock together with staging after a crash, construction attempt, uncertain failure, or unverifiable state; later invocations refuse it until explicit human resolution. The lock contains no PID, timestamp, host, user, prose, or other invocation metadata.
- **Guarantee within Models A/B:** Validate inputs and bytes before writing; create staged regular files exclusively without following symlinks; retain descriptor-relative construction as defense-in-depth; make exact reruns idempotent; refuse mismatching, partial, stale, symlinked, or unexpected state; preserve failed staging and uncertainty; atomically refuse an existing final destination; and verify the complete published tree afterward.
- **Boundary:** The publication lock coordinates cooperating SebGPT publishers only. It is not a security boundary and does not prove that an unrelated same-user process cannot mutate `data`. Existing descriptor, identity, no-follow, and no-clobber checks remain useful hardening but must not be described as proof of Model-C security. This amendment supersedes only DEC-0012's threat-model scope; its byte identity, artifact authority, idempotence, failure-preservation, generated-manifest, and production-authorization decisions remain unchanged.

## DEC-0013 — First tokenizer conceptual direction

- **Date:** 2026-09-08
- **Status:** Accepted for Phase 2 design; implementation and production vocabulary construction are not authorized
- **Decision:** SebGPT's first tokenizer uses one Python `str` iteration element, identified by its Unicode code-point integer, per token. It performs no Unicode normalization. It derives and freezes its vocabulary exclusively from the six accepted training works, orders code points by ascending integer value, and assigns contiguous zero-based IDs in that order. Validation cannot add entries, and the sealed test work does not participate. The tokenizer has no unknown, beginning, end, padding, document-separator, reserved, or unused token IDs. An unrecognized code point raises a deterministic content-safe error without replacement, removal, normalization, or vocabulary extension. Supported text, including the empty string and exact whitespace, must satisfy `decode(encode(text)) == text`. Every work remains an independent token stream. Phase 2 leaves the sealed test untokenized and uninspected. Context windows are outside the tokenizer implementation; Phase 4 should introduce next-token window semantics and Phase 8 production sampling and batching unless a later decision changes that boundary. Byte-level and BPE tokenizers are possible later educational comparisons only.
- **Why:** A code-point tokenizer makes token units, vocabulary construction, integer assignment, encoding, decoding, failure behavior, and round trips directly inspectable. The training inventory is small, while the accepted zero-unseen-code-point observation shows corpus compatibility without making test data a vocabulary source or eliminating the need for a generic unknown-input policy. Strict errors and no special tokens preserve exact reversibility and avoid introducing later modeling choices prematurely.
- **Alternatives considered:** Byte-level tokenization; word tokenization; BPE, WordPiece, or Unigram subwords; an unknown token; special document or batching tokens; frequency or first-occurrence ID order; Unicode normalization; deriving vocabulary from all splits.
- **Consequences:** The eventual vocabulary size equals the number of distinct training code points and contains no additional entries. Production vocabulary construction, real ID assignment, implementation, encoding, and statistics remain separately gated. The sealed-test prohibition is stricter during Phase 2 than merely hiding prose: no tokenizer operation or statistic may be applied to it.

### Scope correction (2026-09-08) — Accepted conceptual boundary only

- **Review result:** The first independent contract review found that the original DEC-0013 text mixed accepted conceptual choices with proposed detailed mechanics. The original text above is preserved as append-only history; this correction is the current authority for what DEC-0013 accepts.
- **Accepted scope:** DEC-0013 accepts only: one Python Unicode code point per token; training-only vocabulary membership; numeric Unicode code-point ordering determines IDs; no Unicode normalization; no special tokens initially; strict rejection of unsupported code points rather than an unknown token; exact round trips for supported text; independent documents; and no context-window construction in Phase 2.
- **Proposed rather than accepted:** Exact freeze mechanics, contiguous zero-based implementation details, error representation, API and container rules, builder architecture, artifact schema and provenance, statistics, exact Phase 4/Phase 8 allocation, future byte/BPE comparison language, gate mechanics, and every other implementation-level detail belong to corrected DEC-0014 until separately reviewed and accepted.
- **Consequence:** Any broader wording in the original DEC-0013 entry must be read as superseded proposal, not as an accepted decision. Production vocabulary construction, tokenization, statistics, and implementation remain unauthorized.

## DEC-0014 — Detailed code-point tokenizer and vocabulary contract

- **Date:** 2026-09-08
- **Status:** Proposed; under independent review
- **Decision:** Adopt `docs/TOKENIZER_SPEC.md` as the detailed Phase 2 contract. Represent tokenizer state canonically as one strictly increasing immutable tuple of code-point integers; derive decode identity from tuple position and use binary search for encoding rather than expose redundant mutable dictionaries. Accept one `str` for encoding and one tuple of exact integer IDs for decoding, return immutable tuples, support empty inputs, reject coercion, and use content-safe unknown-code-point and invalid-ID errors with zero-based positions. Build the vocabulary from exactly six independently validated training `ExtractedWork` objects without rereading files or concatenating text. Later persist the reviewed production mapping as deterministic, literal-free UTF-8 JSON at `artifacts/tokenizers/shakespeare-code-point-v1/vocabulary.json`, Git-track it, hash its exact bytes, and bind it to the Phase 1 manifest, training document hashes, and reviewed builder source. Inspect training and aggregate validation statistics only; do not tokenize or inspect the sealed test during Phase 2. Follow the nine Phase 2 gates recorded in the specification and roadmap.
- **Why:** One immutable ordered tuple is sufficient to explain and reconstruct both directions of the mapping. Strict input structures make silent coercion impossible. A small tracked JSON artifact makes the exact model-critical mapping recoverable and reviewable, while integer and `U+` fields avoid ambiguous literal whitespace and control characters. Direct use of the accepted in-memory Phase 1 handoff preserves provenance and prevents duplicated extraction.
- **Alternatives considered:** Expose mutable forward and reverse dictionaries; accept arbitrary iterables or tensors; filter all eight works inside the builder; reread processed files; store literal characters or Unicode names; store the vocabulary in executable Python; leave the mapping untracked and regenerate it at runtime; inspect sealed-test aggregates during Phase 2; construct context windows with tokenization.
- **Consequences:** Independent review must accept or amend the proposed API, error fields, builder boundary, artifact schema/path/serialization, authority rules, statistics scope, and gates before implementation can be authorized. No tokenizer source, production vocabulary, IDs, encoded corpus, or statistics are created by this decision entry.

### Proposed correction after independent review (2026-09-08)

- **Review result:** FAIL with six contract corrections required. Sebastien accepted the findings and authorized documentation correction only.
- **Corrected proposal:** Replace the original DEC-0014 proposal with the corrected `docs/TOKENIZER_SPEC.md` contract for review. The corrected proposal includes the full Python integer domain from `0` through `0x10FFFF`, including surrogates; left-to-right first-failure encoding; `Sequence[int]` decoding with explicit exclusions and first-invalid-element behavior; one immutable strictly increasing code-point tuple as canonical state; a corpus-neutral membership function plus a Shakespeare-specific production orchestrator; metadata-before-text sealed-test enforcement; an exact deterministic JSON vocabulary schema using a reviewed clean implementation Git commit rather than a source-file hash; an explicit artifact authority chain; ephemeral training and aggregate validation statistics only; proposed rather than accepted Phase 4/Phase 8 allocation; and nineteen separate Phase 2 gates.
- **Status:** Proposed; corrected and awaiting focused independent re-review. DEC-0014 is not accepted. The original proposal above remains append-only history and is superseded by this corrected proposal for review.
- **Consequence:** No implementation, production vocabulary, artifact, token IDs, corpus tokenization, statistics, context windows, or later-phase work is authorized.

### Acceptance update (2026-09-08) — Corrected detailed contract accepted

- **Review result:** Focused independent re-review passed with no must-fix issues. Sebastien explicitly accepted the corrected DEC-0014 detailed contract after two non-semantic wording refinements.
- **Wording refinements:** Decode input acceptance is exactly `isinstance(token_ids, collections.abc.Sequence)` except that `str`, `bytes`, and `bytearray` are rejected; no PyTorch-specific tokenizer logic is introduced. Access-guard tests distinguish vocabulary construction, which rejects validation and test objects before text access, from statistics, which may access training and aggregate-only validation text but rejects test objects before text access.
- **Current status:** Accepted. The corrected `docs/TOKENIZER_SPEC.md` is the authoritative detailed tokenizer contract. The original DEC-0014 proposal and correction remain above as append-only review history.
- **Gate status:** Gate 3 is complete. Gate 4, the accepted contract checkpoint commit, is explicitly authorized. Implementation, production vocabulary construction, tokenization, statistics, context windows, and later phases remain unauthorized.

## DEC-0015 — First embedding and positional-representation direction

- **Date:** 2026-09-08
- **Status:** Conceptual architecture accepted; detailed contract proposed for independent review
- **Accepted decision:** Use an embedding dimension of 32. Represent the accepted 81-token vocabulary with one explicit learnable token table of shape `(81, 32)` and 2,592 parameters. Use learned absolute positional embeddings with capacity 256, rows `0` through `255`, shape `(256, 32)`, and 8,192 parameters. Add token and position vectors directly, for 10,784 total representation parameters. Implement the future mechanism as one small `torch.nn.Module` containing exactly two manually managed `torch.nn.Parameter` matrices rather than `torch.nn.Embedding`. Initialize the token table first and position table second from the same local CPU `torch.Generator` seeded with 1337, using explicit normal initialization with mean zero and standard deviation `1 / sqrt(32)`. The semantic baseline is CPU `torch.float32` parameters and `torch.long` IDs. Bind construction to tokenizer ID `shakespeare-code-point-v1`, artifact schema 1, vocabulary size 81, canonical artifact path `artifacts/tokenizers/shakespeare-code-point-v1/vocabulary.json`, and artifact SHA-256 `9f4235f9dab3e0361221a90c5ae7ca7fee540760fcc1708964368e6f17d6d70e`; size alone is insufficient. Tiny synthetic single-sequence and batched lookup, gradient, and one-hot-equivalence demonstrations are permitted during later authorized implementation. Maximum positional capacity 256 does not select training context length.
- **Proposed detailed mechanics:** Adopt `docs/EMBEDDING_SPEC.md` as the detailed Phase 3 contract only after independent review, any required correction, and explicit acceptance. Its proposed mechanics define one combined module and `forward` API, a tokenizer-owned immutable vocabulary binding, exact tensor validation and empty-shape behavior, internal position generation, output semantics, local-generator initialization order, CPU bitwise-repeatability scope, two content-safe exception types, thirty-six contract checks, later-phase exclusions, and eighteen separate gates. These details are not accepted merely because they are documented.
- **Why:** A manually indexed parameter table exposes the mathematical mechanism, registration, initialization, shapes, shared rows, and gradient accumulation directly. Width 32 is a small but useful educational representation size. Learned absolute positions reuse the same lookup concept and remain easy to inspect. An exact artifact identity prevents an 81-row table from silently acquiring different token meanings. Local deterministic initialization separates model identity from unrelated global random-number use.
- **Alternatives considered:** Embedding dimensions 8, 16, 64, or 128; `torch.nn.Embedding`; one-hot production inputs; separate token and position modules; sinusoidal positions; RoPE before attention; an unspecified or training-derived positional capacity; implicit initialization; global RNG state; MPS or mixed precision as the baseline; accepting vocabulary size without exact mapping identity; loading Phase 1 corpus data during runtime model construction.
- **Consequences:** Phase 3 contract design is authorized and its accepted constants may not be silently changed. Implementation, parameter construction, tests, context windows, production batching, next-token targets, model/loss/attention work, training, and generation remain unauthorized. The detailed API, authority loader, validation, error, determinism, and test mechanics require independent review before a contract commit or implementation authorization.

### Proposed correction after independent review (2026-09-08)

- **Review result:** FAIL with six detailed-contract corrections required. The reviewer explicitly left the accepted DEC-0015 conceptual architecture unchanged. Sebastien authorized documentation-only correction without implementation, staging, commit, or push.
- **Corrected proposal:** Revise `docs/EMBEDDING_SPEC.md` so a tokenizer-owned corpus-free loader verifies the pinned hash plus the complete accepted Phase 2 artifact schema, semantics, provenance, exact integer types, ID records, and U+ notation before factory-producing an immutable binding; the embedding layer consumes that binding without artifact I/O or semantic revalidation. Accept `torch.Tensor` subclasses through `isinstance` while preserving every mathematical input invariant. Use explicit generated `torch.long` position IDs in authoritative lookup pseudocode. Limit bitwise initialization identity to independent constructions under the same supported runtime, platform, and build. Require test-local literals or independent derivation for critical expectations, including tensor-subclass, `meta` wrong-device, and verified-binding boundary coverage. Make focused independent re-review mandatory after contract or implementation corrections and allow acceptance only after a passing independent result.
- **Current status:** Corrected proposal awaiting focused independent re-review. The accepted conceptual architecture remains in force; none of the corrected detailed mechanics is accepted merely because it is documented.
- **Consequence:** Gate 4's initial review is complete with a FAIL result. Gate 5 corrections are complete locally, but contract acceptance, commit, push, implementation authorization, source, tests, parameters, and later-phase work remain unauthorized until focused independent re-review passes and Sebastien explicitly accepts the corrected contract.

### Acceptance update (2026-09-08) — Corrected detailed contract accepted

- **Review and adjudication result:** Focused independent re-review returned PASS with all six findings resolved. Master-chat adjudication also returned PASS, and Sebastien explicitly accepted the corrected `docs/EMBEDDING_SPEC.md` detailed contract.
- **Accepted exception ownership:** The contract has exactly three public exception boundaries. Tokenizer-owned `VocabularyArtifactError` handles canonical artifact path, bytes, schema, semantics, provenance, hash, and binding-factory failures. Embedding-owned `EmbeddingTypeError` handles a non-tensor `forward` argument. Embedding-owned `EmbeddingContractError` handles an invalid verified-binding argument, configuration, rank, dtype, device, token range, or positional-capacity failure. The embedding constructor and `forward` do not perform artifact I/O, hashing, parsing, or semantic revalidation.
- **Current status:** The corrected Phase 3 conceptual architecture and detailed contract are accepted. Historical proposal and correction text above remains append-only review history.
- **Gate status:** Gate 5 is complete. Gate 6, the dedicated accepted-contract commit, requires separate explicit authorization. Push, implementation authorization, implementation, source, tests, parameters, context windows, and later-phase work remain unauthorized.

### Gate 6 authorization update (2026-09-08)

- **Authorization:** Sebastien explicitly authorized staging exactly `docs/EMBEDDING_SPEC.md`, `DECISIONS.md`, `PROJECT_STATE.md`, `ROADMAP.md`, `README.md`, and `LEARNING_NOTES.md` and creating the dedicated accepted-contract commit with message `Define Phase 3 embedding contract`.
- **Gate status:** This dedicated commit completes Gate 6. Gate 7 push/remote verification and Phase 3 implementation remain separately gated and unauthorized.

### Gate 7–9 implementation status update (2026-09-08)

- **Gate 7:** The accepted contract commit `0e458cc8b8bce9d8e89b23abbb8ce6385669b7d5` was pushed, and local `HEAD`, refreshed `origin/main`, and the independently queried remote `main` were verified at that exact commit with a clean synchronized worktree.
- **Gate 8:** Sebastien explicitly authorized Phase 3 implementation strictly under the accepted contract. This did not authorize Phase 4 work, implementation acceptance, commit, or push.
- **Gate 9:** The tokenizer-owned corpus-free vocabulary verifier and immutable binding, the explicit token-plus-position module, and the accepted synthetic checks are implemented locally. Thirty-five automated tests cover contract checks 1–35; contract check 36 is a completed manual phase-boundary review. Implementation remains unaccepted, unstaged, and uncommitted pending master-chat sanity checking and independent Gate 10 review.

### Gate 10–11 implementation acceptance update (2026-09-08)

- **Independent review result:** PASS with zero must-fix issues, zero required corrections, no accepted-contract contradiction, and no Phase 4 or later leakage. The review passed the vocabulary verifier and binding, artifact ordering and safety, module and constructor structure, initialization and RNG isolation, input/length/ID validation, empty shapes, position generation, output semantics, gradients, error ownership, test independence, all thirty-six contract checks, documentation classification, and phase boundary.
- **Regression:** The independent review reproduced 7/7 vocabulary-runtime tests, 28/28 embedding tests, 35/35 automated Phase 3 tests, and the complete 157-test suite with 156 passes, one expected restricted-context MPS skip, zero failures, and a passing `git diff --check`.
- **Acceptance:** Master-chat implementation adjudication returned PASS, and Sebastien explicitly accepted the reviewed Phase 3 implementation. No correction or focused re-review was required.
- **Gate status:** Gates 10 and 11 are complete. Gate 12, the dedicated accepted-implementation commit, requires separate explicit authorization. Commit, push, Phase 3 exit/closure operations, and Phase 4 remain unauthorized.

### Gate 12 authorization update (2026-09-08)

- **Authorization:** Sebastien explicitly authorized staging exactly the six accepted Phase 3 status documents, the three accepted implementation files, and the two accepted test files, then creating the dedicated commit with message `Implement Phase 3 embeddings`.
- **Integrity boundary:** The five source/test files must remain byte-identical to the independently reviewed and accepted checkpoint. No behavior, tests, optional improvements, contract semantics, Phase 4 work, amend, or push is authorized.
- **Gate status:** This dedicated commit completes Gate 12. Gate 13 implementation push/remote verification and every remaining Phase 3 exit/closure operation remain separately gated; Phase 4 remains unauthorized.

### Gate 13–16 Phase 3 closure update (2026-09-08)

- **Gate 13:** Implementation commit `68b47bb404d55357cadce35b97036c72c5876d62` was pushed successfully. Local `HEAD`, refreshed `origin/main`, and independently queried remote `main` matched that exact commit with a clean synchronized worktree.
- **Gate 14:** Explicitly adjudicated SKIP/not required. Existing implementation, tests, documentation, learning notes, one-hot equivalence, rank-1/rank-2 examples, gradient demonstrations, and independent review already supply the required educational evidence; no additional demonstration or inspection was needed.
- **Gate 15:** Independent Phase 3 exit review returned PASS, master-chat adjudication returned PASS, and Sebastien explicitly accepted the result. Each exact roadmap criterion passed: token embeddings and an explicit positional representation are implemented; tensor shapes and parameter roles are explainable and tested; and small examples demonstrate lookup, batching, and gradient flow.
- **Closure status:** The Phase 3 goal, `Understand learned token and positional representations`, is achieved. Phase 3 is technically complete with no remaining technical work. The accepted contract and implementation are unchanged. Sebastien explicitly authorized the documentation-only Gate 16 closure commit with message `Complete Phase 3 embeddings`.
- **Boundary:** Gate 17 push/remote verification is not authorized. Phase 4 remains unauthorized until Gate 17 completes and Sebastien later gives explicit Gate 18 authorization.
